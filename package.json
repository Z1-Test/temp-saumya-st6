{
  "name": "firebase-firestore-seeding",
  "version": "1.0.0",
  "description": "Firebase Firestore data seeding with import-products-polars",
  "scripts": {
    "emulators:start": "firebase emulators:start",
    "emulators:export": "firebase emulators:start --export-on-exit=./firestore-data",
    "seed:data": "node seed-firestore-data.js"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "nodejs-polars": "^0.9.0",
    "commander": "^11.0.0",
    "csv-parser": "^3.0.0"
  },
  "devDependencies": {
    "firebase-tools": "^13.0.0"
  }
}
